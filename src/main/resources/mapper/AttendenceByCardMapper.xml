<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.cms.mapper.AttendanceMapper">
    <insert id="insertAccessRecord" parameterType="com.example.cms.bean.AttendanceByCardBean">
        INSERT ignore INTO `attendence_card` (phoneNum, name, face, community, building, longitude, latitude)
        VALUES (#{phoneNum}, #{name},#{face},#{community},#{building},#{longgitude},#{latitude})
    </insert>
</mapper>